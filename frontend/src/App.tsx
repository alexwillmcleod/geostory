import { IonApp } from '@ionic/react'
import { IonReactRouter } from '@ionic/react-router'

function App() {
  return (
    <IonApp>
      <IonReactRouter>
      </IonReactRouter>
    </IonApp>
  )
}

export default App
